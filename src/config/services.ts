/**
 * Switch this to false once the backend endpoint is available.
 * Every page will then use the API implementation of the fantasy service.
 */
export const serviceConfig = {
  useMockServices: true,
} as const;
