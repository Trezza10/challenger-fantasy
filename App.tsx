import { AppNavigator } from './src/navigation/AppNavigator';

/** Application entry point. Navigation owns the currently displayed page. */
export default function App() {
  return <AppNavigator />;
}
